# URL base de la API de Laravel
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api

# Token público de Mapbox (pk.*). Obtenlo en https://account.mapbox.com/access-tokens/
# Es un token público de cliente; está pensado para exponerse en el navegador.
NEXT_PUBLIC_MAPBOX_TOKEN=
